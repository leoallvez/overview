package br.dev.singular.overview.presentation.tagging.params

object TagStreaming {
    const val PATH = "/select-streaming"

    object Detail {
        const val STREAMING_CHANGE = "streaming-change"
    }
}
