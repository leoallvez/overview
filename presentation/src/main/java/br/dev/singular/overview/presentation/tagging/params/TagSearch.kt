package br.dev.singular.overview.presentation.tagging.params

object TagSearch {
    const val PATH = "/search"
    const val PATH_SUGGESTIONS = "$PATH:suggestions"

    object Detail {
        const val SEARCH_FIELD = "search-field"
        const val CLEAN_SEARCH_FIELD = "clean-search-field"
    }
}
